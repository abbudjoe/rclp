name: conflicting_speed_alias_denied
description: Conflicting command speed aliases under a speed-limited lease fail closed.
kind: command_gate
now_unix_ms: 1760000000000
input:
  include_current_state: true
  lease:
    constraints:
      max_speed_mps: 0.5
  command:
    payload:
      max_speed_mps: 0.4
      speed_mps: 99.0
expected:
  decision: deny
  reason_code: COMMAND_SPEED_CONFLICT
