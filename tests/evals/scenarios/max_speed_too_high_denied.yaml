name: max_speed_too_high_denied
description: A command exceeding the lease max_speed_mps constraint fails closed.
kind: command_gate
now_unix_ms: 1760000000000
input:
  policy:
    requirements:
      max_speed_mps: 0.5
  include_current_state: true
  lease:
    constraints:
      max_speed_mps: 0.5
  command:
    payload:
      max_speed_mps: 0.6
expected:
  decision: deny
  reason_code: COMMAND_SPEED_TOO_HIGH
