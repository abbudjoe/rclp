name: nonfinite_speed_denied
description: Non-finite command speed under a speed-limited lease fails closed.
kind: command_gate
now_unix_ms: 1760000000000
input:
  policy:
    requirements:
      max_speed_mps: 0.5
  include_current_state: true
  lease:
    constraints:
      max_speed_mps: 0.5
  command:
    payload:
      max_speed_mps: .nan
expected:
  decision: deny
  reason_code: COMMAND_SPEED_MALFORMED
