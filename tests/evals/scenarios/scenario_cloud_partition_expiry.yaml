name: scenario_cloud_partition_expiry
description: During a partition, current-state command gating fails closed before expiry and late authority is denied after expiry.
kind: cloud_partition_expiry
now_unix_ms: 1760000000000
expected:
  decision: deny
  reason_code: LEASE_EXPIRED
  audit_event_types:
    - capability_allowed
    - command_allowed
    - network_state_asserted
    - capability_denied
    - command_rejected
    - fallback_declared
  audit_required_fields:
    - event_id
    - timestamp
    - decision
    - reason_code
    - central_agent_id
    - edge_agent_id
    - robot_id
    - mission_id
    - requested_capability
    - lease_id
    - network_state
    - geofence_state
    - fallback_action
